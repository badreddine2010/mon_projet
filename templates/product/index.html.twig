{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}
    <h1>Product index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Category</th>
                <th>Quantité</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td>{{ product.id }}</td>
                <td>{{ product.name }}</td>
                <td>{{ product.category.name }}</td>
                <td>{{ product.quantity }}</td>
                <td>{{ product.price }}</td>
                <td>
                    {# <a class="btn btn-warning" href="{{ path('app_product_show', {'id': product.id}) }}">show</a> #}
                    {# <a class="btn btn-success" href="{{ path('app_product_edit', {'id': product.id}) }}">edit</a> #}
                    <a class="btn btn-success" href="{{path('cart_add',{'id':product.id})}}">Ajouter panier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a class="btn btn-primary" href="{{ path('app_product_new') }}">Create new</a> #}
{% endblock %}
